numpy==1.26.4
pandas>=1.3.0
matplotlib>=3.4.0
seaborn>=0.11.0
pytest>=7.0.0
scikit-learn>=1.0.0
tqdm>=4.62.0
pyarrow>=5.0.0
pygame>=2.1.0
tfrecord>=0.3.0
protobuf>=3.20.0
joblib>=1.4.0
tabulate>=0.9.0

# Note: shap is intentionally omitted here because nuscenes-devkit 1.2.0
# requires numpy<2.0.0, while recent shap wheels require numpy>=2.0.0.
# If SHAP explainability is needed later, install a pre-built shap wheel
# compatible with numpy 1.26.4, or use a separate environment.
